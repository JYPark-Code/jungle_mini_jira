# git 원격저장소 fetch
```
# 기본 사용법: origin 저장소에서 변경 사항 가져오기
git fetch origin
```

# git branch 전환
```
git checkout
```

# 가상환경 활성화
```
1. python -m venv .venv
2. source .venv/bin/activate (Shell 환경)
2. .venv\Scripts\activate (PowerShell)
```

# requirements.txt 사용법

```
pip freeze > requirements.txt

pip install -r requirements.txt
```
